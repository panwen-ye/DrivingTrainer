# 驾考训练 MVP 集成测试报告

报告日期：2026-08-19  
测试版本：本地 `master`（Personal Team 配置之后）  
测试环境：macOS、Xcode 26.6、iPhone 17 Simulator / iOS 26.5、watchOS 26.5 SDK

## 结论

基础 MVP 软件验收通过。

- 自动化测试：21 项通过，0 项失败。
- Apple 工程构建：通过，0 个编译错误，0 个编译警告。
- 模拟器安装与启动：通过。
- 模拟器端到端用例：14 项软件路径均通过。
- 高优先级未解决缺陷：0。
- 真机用例：5 项待执行；原因是当前 Mac 尚未检测到已连接并信任的 iPhone，Personal Team 显示 0 台 Provisioned Devices。

## 自动化结果

| 编号 | 结果 | 实际验证 |
|---|---|---|
| IT-A01 | 通过 | 含两节点路线保存、重载、顺序提醒、完成/困难标记、训练记录保存及重载全部符合预期 |
| IT-A02 | 通过 | 同 ID 路线编辑后未产生重复项，版本与节点正确更新 |
| IT-A03 | 通过 | 多条训练记录重载后按开始时间倒序排列 |
| IT-A04 | 通过 | Domain、Persistence 与 Integration 共 21 项测试，0 失败 |
| IT-A05 | 通过 | DrivingTrainerApp scheme 构建成功，内嵌 Watch App 一并通过 |

自动化执行摘要：

```text
Executed 21 tests, with 0 failures in 0.018 seconds
Build Succeeded
```

## 模拟器端到端结果

| 编号 | 结果 | 实际观察 |
|---|---|---|
| IT-U01 | 通过 | 清洁安装后显示安全提示及 1、3、4 号路线，无错误弹窗 |
| IT-U02 | 通过 | 系统展示中文定位用途说明；选择“使用 App 时允许”后地图正常 |
| IT-U03 | 通过 | 0 节点路线显示“开始训练”禁用 |
| IT-U04 | 通过 | 录制页显示名称、距离、轨迹点、节点与开始按钮 |
| IT-U05 | 通过 | 开始后显示暂停；暂停后显示继续；继续后恢复采集，轨迹点由 1 增至 2 |
| IT-U06 | 通过 | 节点表单正常打开，默认类型为自定义、口令为“前方考试节点”、半径 120 米 |
| IT-U07 | 通过 | 保存节点后计数从 0 变为 1 |
| IT-U08 | 通过 | “新路线 · 1 个节点”出现在列表；停止并重新打开 App 后仍存在 |
| IT-U09 | 通过（软件路径） | 进入节点半径后显示“已提醒：距离节点约 0 米”，中文播报和通知反馈代码路径被触发；实际震感归入真机用例 |
| IT-U10 | 通过 | 标记“有困难”后节点完成；结束后明确显示“训练记录已保存” |
| IT-U11 | 通过 | 补齐 `WKCompanionAppBundleIdentifier` 后，iPhone 包成功安装并运行 |
| IT-U12 | 通过 | Watch 未配对时仅产生 `WCSession is not paired` 提示，iPhone 全流程无崩溃 |
| IT-U13 | 通过 | 训练地图显示带中文辅助功能标签的放大/缩小按钮；点击放大后比例尺由约 36 米变为约 15 米。代码同时明确启用双指缩放 |
| IT-U14 | 通过 | 路线轨迹和节点共同生成带边距的初始 `MKMapRect`；模拟器进入训练页时节点位于有效视野内，空数据分支回退用户位置 |

## 测试期间发现并修复的问题

| 缺陷 | 严重度 | 修复与回归结果 |
|---|---|---|
| ISO 8601 日期编码丢失亚秒精度，保存重载后严格比较失败 | 中 | 改用 Codable 默认日期表示；存储及集成测试通过 |
| WatchConnectivity 字典跨 MainActor 导致 Swift 6 数据竞争编译错误 | 高 | 在非隔离回调中提取 Sendable 值后切换 MainActor；重新构建通过 |
| Watch App 缺少 iPhone companion bundle identifier，导致模拟器安装失败 | 高 | 在工程配置加入 `com.panwenye.DrivingTrainer`；重新安装运行通过 |
| Xcode/iOS/watchOS 开发组件缺失 | 环境 | 安装 Xcode 26.6、iOS 26.5 和 watchOS 26.5 组件；构建通过 |
| `MapZoomStepper` 在 iOS 不可用 | 高 | 改为绑定 `MapCameraPosition` 的原生 SwiftUI 加减按钮；iPhone 目标重新编译成功并在模拟器确认比例尺变化 |

## 尚待真机执行

| 编号 | 状态 | 阻塞条件 |
|---|---|---|
| IT-D01 自动签名与安装 | 待真机 | 连接、解锁并信任 iPhone |
| IT-D02 开发者模式 | 待真机 | iPhone 首次安装时由用户确认系统授权 |
| IT-D03 室外 GPS 短测 | 待真机 | 需要室外步行或安全停车环境测试 5–10 分钟 |
| IT-D04 Watch 联动 | 待真机 | 需要已配对 Apple Watch |
| IT-D05 驾驶安全验收 | 待实地 | 必须由副驾或停车状态下操作，驾驶员不得在行驶中触屏 |

## Apple 地图路线导入增量验证

| 编号 | 状态 | 实际验证 |
|---|---|---|
| IT-U15 | 软件路径通过，真链接待真机 | iPhone 目标编译成功；模拟器确认入口、目标路线选择、链接输入和提交状态正常。`MKDirections` 实际路线结果需用用户从 Apple 地图复制的真实分享链接在联网真机复核 |
| IT-U16 | 通过 | 非法/非 Apple 域名、缺少起终点、地点无法解析和无驾驶路线均有独立中文错误；错误发生前不会调用 `saveRoute` |

## 地图缩放交互增量验证

| 编号 | 状态 | 实际验证 |
|---|---|---|
| IT-U17 | 通过 | iPhone 17 模拟器确认控件为右下角 42 点宽纵向浮层，不再横向遮挡地图；点击放大后比例尺由约 36 米变为约 12 米，动画正常 |
| IT-U18 | 代码路径通过，外接鼠标待真机 | 已为底层 `MKMapView` 安装仅接收 continuous/discrete scroll 的手势识别器，并允许与原生手势同时识别。自动化模拟器无法可靠合成外接鼠标硬件滚轮事件，需连接鼠标进行最终验收 |

## 发布建议

当前版本适合作为个人 MVP 进入真机测试，不建议直接用于正式道路驾驶决策。完成 IT-D01 至 IT-D04 后，可判定“可安装真机版”验收完成。Personal Team 免费签名通常需要周期性重新安装，属于个人测试分发限制。
